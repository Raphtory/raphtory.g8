ThisBuild / name := "$name$"
ThisBuild / version := "$version$"
ThisBuild / organization := "$organization$"
ThisBuild / scalaVersion := "$scala_version$"

libraryDependencies ++= Seq("com.raphtory" %% "core" % "$raphtory_version$")
